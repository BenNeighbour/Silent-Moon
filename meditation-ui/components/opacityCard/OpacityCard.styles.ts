import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        display: "flex",
        width: "100%",
        borderRadius: 10,
        backgroundColor: "black",
        overflow: "hidden"
    },
    title: {
        color: "#FFFFFF",
        fontSize: 18,
        fontWeight: "bold"
    }
});