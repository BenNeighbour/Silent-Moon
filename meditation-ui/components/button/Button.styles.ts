import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        display: "flex",
        elevation: 0,
        borderRadius: 40,
        textAlign: "center",
        alignItems: "center",
        width: "100%"
    },
    text: {
        fontSize: 14,
        textTransform: "uppercase",
        fontWeight: "normal",
    }
});