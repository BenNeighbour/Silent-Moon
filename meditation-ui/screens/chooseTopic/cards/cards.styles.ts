import { StyleSheet } from "react-native";

export let styles = StyleSheet.create({
    container: {
        display: "flex",
        borderRadius: 10,
        marginVertical: 10,
        overflow: "hidden"
    },
    title: {
        padding: 16,
        paddingTop: 0,
        marginTop: 0,
        width: "100%",
        fontWeight: "bold",
        fontSize: 18,
    },
});
